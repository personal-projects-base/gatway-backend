spring:
  cloud:
    gateway:
      routes:
        - id: church-lite-service-route
          uri: http://localhost:5050
          predicates:
            - Path=/api/**
          filters:
            - RewritePath=/api/(?<segment>.*), /${segment}
  application:
    name: api-gateway

server:
  port: 5000